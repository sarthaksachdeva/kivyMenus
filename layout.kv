#:import RiseInTransition kivy.uix.screenmanager.RiseInTransition
#:import WipeTransition kivy.uix.screenmanager.WipeTransition
bxL:
    orientation: 'vertical'
    padding: 5
    spacing: 5
    #ROW 1
    BoxLayout:
        spacing: 5
        orientation: 'horizontal'
        size_hint: 1,0.5

        FocusButton:
            on_press: root.parent.parent.load_next()
            background_color: C('#3498DB')
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/milkCollection2.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 14
                    text_size: self.size
                    text:  root.getText('oprtn1')
        #MILK COLLECTION
        FocusButton:
            nextScreen: 'firstCollectionMenu'
            transition: RiseInTransition()
            on_release:
                app.switchScreen('firstCollectionMenu', self.transition)
                self.focus = False
            background_color: C('#2ECC71')
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/operationsOutbound.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 14
                    text_size: self.size
                    text:  root.getText('oprtn2')
        # ADD CUSTOMER
        FocusButton:
            nextScreen: 'CustomerMenu'
            transition: RiseInTransition()
            on_release:
                app.switchScreen('CustomerMenu', self.transition)
                self.focus = False
            background_color: C('#1ABC9C')
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/union.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 14
                    text_size: self.size
                    text:  root.getText('oprtn3')

    #ROW 2
    BoxLayout:
        spacing: 5
        orientation: 'horizontal'
        size_hint:1,0.25
        FocusButton:
            background_color: C('#27AE60')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/reports2.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpindl')
        FocusButton:
            background_color: C('#16A085')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/reports3.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpinmn')
        FocusButton:
            background_color: C('#E74C3C')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/reports4.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpinan')
        FocusButton:
            nextScreen: 'CustomerDb'
            transition: RiseInTransition()
            on_release:
                app.switchScreen('CustomerDb', self.transition)
                self.focus = False
            background_color: C('#95A5A6')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/reports6.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpinct')
        FocusButton:
            attachedGif: reportsImg
            animDelay: 0.08
            background_color: C('#3498DB')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    id: reportsImg
                    source: './images/reports8.gif'
                    anim_delay:-1
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpinan')
        FocusButton:
            background_color: C('#F3b200')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/customer2.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpinct')
    # ROW 3
    BoxLayout:
        spacing: 5
        orientation: 'horizontal'
        size_hint: 1,0.25

        #CUSTOMER
        FocusButton:
            attachedGif: customerImg
            animDelay: 0.02
            background_color: C('#00D8cc')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    id: customerImg
                    source: './images/reports9.gif'
                    anim_delay: -1
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpotdl')
        FocusButton:
            background_color: C('#FF1D77')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/reports10.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpotmn')

        FocusButton:
            background_color: C('#632F00')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/reports11.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpotan')
        #STOCKS
        FocusButton:
            attachedGif: stockImg
            animDelay: 0.05
            background_color: C('#4617B4')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    id: stockImg
                    source: './images/reports7.gif'
                    anim_delay: -1
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpotst')

        #ANNUAL
        FocusButton:
            background_color: C('#00c13F')
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    source: './images/reports15.png'
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('rpotan')
        #SETTINGS
        FocusButton:
            nextScreen: 'home'
            transition: WipeTransition()
            attachedGif: cubeImg
            animDelay: 0.05
            background_color: C('#000000')
            on_release:
                app.switchScreen('home', self.transition)
                self.focus = False
            on_create:
                self.disabled = False if app.isUserAdmin() else True
            StackLayout:
                pos: self.parent.pos
                size: self.parent.size
                padding: 10
                spacing: 0
                orientation: 'tb-lr'

                Image:
                    id: cubeImg
                    source: './images/cubes.gif'
                    anim_delay: -1
                    size_hint:1,0.8
                Label:
                    size_hint:1,0.2
                    halign: 'left'
                    valign: 'bottom'
                    font_size: 12
                    text_size: self.size
                    text:  root.getText('settng')

